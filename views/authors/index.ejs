<h2>Search Authors</h2>
<form action="">
    <label for="">Name</label>
    <input type="text" value="<%= searchOptions.name %>" name="name">
    <button type="submit">Search</button>
</form>
<br>
<% authors.forEach(author => { %>
    <div><%= author.name %></div>
<% }) %>